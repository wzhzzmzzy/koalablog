---
import type { Markdown } from '@/db/types'
import { marked } from 'marked'

interface Props {
  article: Markdown
}

const { article } = Astro.props

const content = article ? marked.parse(article.content ?? '') : ''
---

<h1>{ article.subject }</h1>
<article set:html={content} />
