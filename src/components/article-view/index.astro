---
import type { Markdown } from '@/db/types'
import { md } from '@/lib/markdown'

interface Props {
  article: Markdown
}

const { article } = Astro.props

const content = article ? await md().renderAsync(article.content ?? '') : ''
---

<h1>{ article.subject }</h1>
<article set:html={content} />
